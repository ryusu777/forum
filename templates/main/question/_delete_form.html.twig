<form method="post" action="{{ path('question_delete', {'idPertanyaan': pertanyaan.idPertanyaan}) }}"
    onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pertanyaan.idPertanyaan) }}">
    <button class="btn">Delete</button>
</form>